[project]
name = "laya-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "vrushabh deshmukh", email = "itsvrushabh@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.0",
    "jinja2>=3.1.0",
    "laya>=0.3.20",
    "rich>=13.0.0",
    "uvicorn>=0.30.0",
]

[project.scripts]
laya-demo = "laya_demo:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
